export interface IProduct {
    id: string
    name: string
    card_img: string
    description: string
    characteristic: string
    specification: string
    additionally: string
    category: string
    subcategory: string
    modification: string
    images: Array<string>
}