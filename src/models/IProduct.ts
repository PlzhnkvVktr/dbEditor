export interface IProduct {
    id: string
    name: string,
    description: string,
    characteristic: string,
    specification: string,
    additionally: string,
    category: number,
    subcategory: number,
    images: Array<string>,
}